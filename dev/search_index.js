var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-Reference","page":"API","text":"","title":"API Reference"},{"category":"section","location":"api/#Module","page":"API","text":"","title":"Module"},{"category":"section","location":"api/#Unified-API","page":"API","text":"One function per task; method=:socp (default), :shooting or :chambolle_pock selects the algorithm. All of geodesic, transport_cost, barycenter and analysis also accept method=:sinkhorn (entropic transport for a ground cost, a different object; cost and epsilon are required; see each docstring). Keywords are forwarded to the chosen implementation.","title":"Unified API"},{"category":"section","location":"api/#SOCP-implementation-(internal)","page":"API","text":"","title":"SOCP implementation (internal)"},{"category":"section","location":"api/#Hamiltonian-shooting-(exp/log-maps)","page":"API","text":"","title":"Hamiltonian shooting (exp/log maps)"},{"category":"section","location":"api/#Chambolle-Pock-implementation-(reference,-internal)","page":"API","text":"","title":"Chambolle-Pock implementation (reference, internal)"},{"category":"section","location":"api/#Sinkhorn:-entropic-barycenters","page":"API","text":"","title":"Sinkhorn: entropic barycenters"},{"category":"section","location":"api/#Graph-constructors","page":"API","text":"","title":"Graph constructors"},{"category":"section","location":"api/#Graph-calculus","page":"API","text":"","title":"Graph calculus"},{"category":"section","location":"api/#Admissible-means","page":"API","text":"","title":"Admissible means"},{"category":"section","location":"api/#Chambolle-Pock-data-structures","page":"API","text":"","title":"Chambolle-Pock data structures"},{"category":"module","location":"api/#GraphTransportation","page":"API","text":"GraphTransportation\n\nJulia package for discrete transport geometry on graphs, implementing the framework of Erbar, Rumpf, Schmitzer, and Simon — Computation of optimal transport on discrete metric measure spaces.\n\nThe package provides:\n\nGeodesics (discrete_transport, transport_cost): compute discrete transport geodesics between probability measures on a graph via a Galerkin-discretised Chambolle-Pock primal-dual algorithm.\nBarycenters (barycenter): compute discrete transport Fréchet means via gradient descent.\nCoordinate recovery (analysis): recover barycentric coordinates of a measure with respect to a reference family by solving a quadratic programme on the Gram matrix of logarithmic maps.\nEntropic optimal transport barycenters (sinkhorn_barycenter, simplex_regression): entropic optimal transport barycenter computation and coordinate recovery via the Sinkhorn algorithm.\n\n\n\n\n\n","title":"GraphTransportation"},{"category":"function","location":"api/#GraphTransportation.geodesic","page":"API","text":"geodesic(G::MarkovGraph, ρA, ρB; method=:socp, kwargs...) -> GeodesicSolution\n\nThe discrete transport geodesic between densities ρA and ρB on G, by one of:\n\n:socp (default): a single second-order-cone program (geodesic_socp). Handles any densities, including boundary-supported ones; time-discretization error O(1/N). Keywords: N, optimizer, silent.\n:shooting: Newton shooting on the Hamiltonian flow (log_map), then the flow is integrated to produce the path. Exact in time, fastest, but requires strictly positive endpoints (PositivityFloorError/assertion otherwise). Keywords: nsteps, tol, maxiters, φ0_init.\n:chambolle_pock: the paper's Galerkin-discretised primal-dual iteration (discrete_transport). Reference implementation; slowest. Keywords: N, tol, maxiters, σ, τ.\n:sinkhorn: the entropic displacement interpolation for a ground cost: the path is the entropic barycenter of the two endpoints at weights (1−t, t) for t = 0, 1/N, …, 1 (the two-reference special case of barycenter(...; method=:sinkhorn)), a different object from the discrete transport geodesic. Requires cost (see ground_cost) and epsilon; keywords N (default 10) and iters (Sinkhorn budget, default 256). W2 is the entropic transport cost ⟨cost, P⟩ of the plan between the endpoints (no entropy term). The path's end columns are the blurred endpoints the Sinkhorn barycenter returns, not ρA/ρB exactly; m, φ0, φ1 are NaN-filled.\n\nThe transport metric's mean is the graph's G.mean (see MarkovGraph): :socp and :shooting honour every AdmissibleMean (the SOCP needs QuadLogMean for the logarithmic mean), :chambolle_pock supports only GeometricMean() and errors otherwise, and :sinkhorn's geometry is its ground cost, so it ignores the mean.\n\nAll methods return a GeodesicSolution. Its W2 is the squared distance; ρ is the n × (steps+1) density path and m the |E| × steps momentum path. The endpoint potentials φ0, φ1 are the gradients of W2 with respect to each endpoint (the SOCP's duals); for :shooting they are derived from the flow's velocity potentials (φ0 = -2φ(0), φ1 = 2φ(1)), and for :chambolle_pock they are not available (NaN-filled). Caveat for :chambolle_pock: its momentum path agrees with the other methods away from the endpoints (to O(1/N)), but the first-interval momentum m0 is a boundary-cell artifact of the Galerkin discretization, off by roughly 50% and not converging with N; it is what the paper's momentum-based analysis uses, and is self-consistent there, but it is not the initial momentum in the sense the other two methods return. status is the JuMP termination status for :socp and :converged for the other methods; solvetime is wall-clock seconds.\n\n\n\n\n\n","title":"GraphTransportation.geodesic"},{"category":"function","location":"api/#GraphTransportation.transport_cost","page":"API","text":"transport_cost(Q, μ, ν; N=64, σ=0.5, τ=0.5, maxiters=2^16, tol=1e-10,\n               progress=false) -> Float64\n\nReturn the discrete transport distance W(μ, ν) on the graph defined by Q, computed as √(action(discrete_transport(Q, μ, ν; ...))).\n\nArguments\n\nQ: row-stochastic Markov transition matrix defining the graph\nμ, ν: probability densities w.r.t. the stationary distribution of Q\nN: number of time steps for the geodesic discretisation (default 64)\nσ, τ: Chambolle-Pock step sizes; must satisfy σ·τ < 1 (default 0.5)\nmaxiters: maximum Chambolle-Pock iterations (default 65536)\ntol: convergence tolerance on the density change between iterates\nprogress: show a progress spinner if true\n\n\n\n\n\ntransport_cost(G::MarkovGraph, ρA, ρB; method=:socp, kwargs...) -> Float64\n\nThe discrete transport distance 𝒲(ρA, ρB) (not squared): sqrt(geodesic(...).W2). See geodesic for the methods and keywords.\n\n\n\n\n\n","title":"GraphTransportation.transport_cost"},{"category":"function","location":"api/#GraphTransportation.barycenter","page":"API","text":"barycenter(M, weights, Q; h=1.0, maxiters=100, tol=1e-8,\n           geodesic_tol=1e-10, geodesic_steps=100,\n           return_stats=false, initialization_index=1,\n           initialization=nothing, uniform_init=false,\n           geodesic_warmstart=true, verbose=true,\n           stagnation_window=100, stagnation_tol=0.01,\n           h_min=1e-8) -> ν  (or (ν, norm_diffs, variances) if return_stats=true)\n\nCompute the discrete transport barycenter of the reference measures in M with respect to weights via gradient descent on the graph defined by Q.\n\nConvergence is assessed on the gradient norm ‖∇J‖; a stagnation detector halves the step size when relative improvement over the last stagnation_window iterates falls below stagnation_tol.\n\nArguments\n\nM: V × p matrix whose columns are reference probability densities\nweights: non-negative length-p vector summing to 1\nQ: row-stochastic Markov transition matrix defining the graph\nh: initial WGD step size (default 1.0)\nmaxiters: maximum number of WGD iterations\ntol: gradient-norm convergence threshold\ngeodesic_tol: convergence tolerance passed to each discrete_transport call\ngeodesic_steps: number of time steps for each geodesic\nreturn_stats: if true, return (ν, norm_diffs, variances) instead of ν\ninitialization: explicit starting measure; takes precedence over index-based init\ninitialization_index: column of M used as the starting point (default 1)\nuniform_init: if true, start from the uniform density (density ≡ 1)\ngeodesic_warmstart: reuse geodesics from the previous iteration as warm starts\nverbose: show a ProgressMeter display with per-iteration statistics\nstagnation_window: number of iterations over which to measure relative improvement\nstagnation_tol: minimum relative improvement before halving h\nh_min: minimum allowed step size; stagnation halving stops here\n\n\n\n\n\nbarycenter(G::MarkovGraph, refs, λ; method=:socp, kwargs...) -> (ν, J, info)\n\nThe discrete transport barycenter of the reference densities refs (a vector of densities on G) with weights λ, i.e. the minimizer of J(ν) = Σᵢ λᵢ 𝒲²(refsᵢ, ν), by:\n\n:socp (default): one joint second-order-cone program (barycenter_socp), solved to its global optimum. info = (; geodesics) holds one GeodesicSolution per reference with λᵢ > 0. Keywords: N, optimizer, silent.\n:shooting: intrinsic (Riemannian) gradient descent with exact-in-time geodesics: each iteration log-maps ν to every reference (log_map, warm-started from the previous iteration), forms the descent direction Σᵢ λᵢ φ0ᵢ as a potential, and moves along the geodesic with exp_map. Requires strictly positive references and works with every AdmissibleMean, including the exact LogarithmicMean, which the SOCP cannot represent. info = (; iters, J_hist, grad_hist, h): the objective and the Riemannian gradient norm per iteration and the final step size. Keywords: h (step, default 1, halved on a positivity-floor hit or an objective increase beyond the log maps' noise, doubled back toward its initial value after an accepted step), maxiters (200), tol (gradient-norm stopping threshold, 1e-7), nsteps (integrator steps, 150), init (starting density, default the λ-weighted average of the references), verbose. First-order, so it converges linearly; the SOCP remains the certificate.\n:chambolle_pock: the paper's intrinsic gradient descent with Chambolle-Pock geodesics (the barycenter(M, weights, Q) method). info = (; norm_diffs, variances) are the descent's per-iteration statistics. Keywords: h, maxiters, tol, geodesic_tol, geodesic_steps, verbose, and the rest of that method's options. J is evaluated afterwards with Chambolle-Pock geodesics at the same settings.\n:sinkhorn: the entropically regularized Wasserstein barycenter for a ground cost (Benamou et al. 2015; Bonneel, Peyré & Cuturi 2016; sinkhorn_barycenter). This is a different object from the discrete transport barycenter of the other methods: it depends on the choice of cost (see ground_cost) and on epsilon, and even for a single reference it returns a blurred copy of that reference. Both cost and epsilon are required keywords; iters (default 256) is the Sinkhorn budget. J = Σᵢ λᵢ ⟨cost, Pᵢ⟩ over the entropic plans Pᵢ (no entropy term) and is not comparable with the J of the other methods; info = (; cost, epsilon, iters, marginal_errors). refs are densities with respect to π as elsewhere (converted to probability vectors internally).\n\nReturns the barycenter ν, the objective value J at ν, and the method-specific info named tuple.\n\n\n\n\n\n","title":"GraphTransportation.barycenter"},{"category":"function","location":"api/#GraphTransportation.analysis","page":"API","text":"analysis(ν, M, Q; N=100, tol=1e-10, compute_condition=false,\n         return_system=false) -> weights\n\nRecover the barycentric coordinates of ν with respect to the reference measures in M (a V × p matrix whose columns are probability densities w.r.t. the stationary distribution of Q).\n\nThe method proceeds in three steps:\n\nFor each reference measure M[:,i], compute the geodesic from ν to M[:,i] via discrete_transport and extract the initial tangent vector m[1,:,:] (the logarithmic map at ν).\nAssemble the p × p Gram matrix A[i,j] = ⟨log_ν(M[:,i]), log_ν(M[:,j])⟩_{g(ν)} under the metric tensor g(ν).\nSolve the simplex-constrained quadratic programme min_{w≥0, Σw=1} w'Aw using Convex.jl / SCS, which recovers the weights at the discrete transport barycenter.\n\nOptional keyword arguments:\n\nN: number of time steps for each geodesic computation (default 100).\ntol: convergence tolerance for each discrete_transport call.\ncompute_condition: if true, prints the condition number of A.\nreturn_system: if true, returns (weights, A) instead of weights alone.\n\n\n\n\n\nanalysis(G::MarkovGraph, target, refs; method=:socp, kwargs...) -> λ̂ (or (λ̂, A))\n\nRecover the barycentric coordinates of target with respect to the reference densities refs: compute the geodesic from target to each reference, build the Gram matrix of their tangent vectors at target, and solve min_{λ∈Δ} λᵀAλ. The geodesic solver is:\n\n:socp (default): analyze_socp — endpoint potentials from the geodesic SOCP. Keywords: N, optimizer, convention.\n:shooting: analyze_shooting — potentials from log_map; interior data only. Keywords: nsteps, tol, φ0_inits.\n:chambolle_pock: the paper's analysis(ν, M, Q) — initial momenta from discrete_transport. Keywords: N, tol.\n:sinkhorn: Wasserstein barycentric coordinates for a ground cost (Bonneel, Peyré & Cuturi 2016; simplex_regression): L-BFGS over the simplex on ½‖P(λ) − target‖², with P(λ) the entropic barycenter of refs, differentiated through the Sinkhorn iterations. Not a Gram-matrix method, so return_system and compute_condition are not supported (an error, not silently ignored). Requires cost and epsilon; keywords iters (Sinkhorn budget, default 256) and α0 (initial pre-softmax point, default 0, i.e. uniform weights). The result depends on cost and epsilon, and recovers exactly the weights of a barycenter synthesized by barycenter(...; method=:sinkhorn) with the same cost, epsilon and iters.\n\nCommon keywords (Gram-matrix methods): compute_condition, return_system (also return the Gram matrix A).\n\nA barycenter is recovered to solver tolerance only by the method (and time resolution) that synthesized it; other methods recover it to their discretization error, because each checks stationarity in its own discrete convention.\n\n\n\n\n\n","title":"GraphTransportation.analysis"},{"category":"type","location":"api/#GraphTransportation.GeodesicSolution","page":"API","text":"GeodesicSolution\n\nResult of solving the geodesic SOCP (geodesic_socp). Note the naming convention: W2 is the squared discrete transport distance; the metric itself is sqrt(W2).\n\nFields:\n\nW2::Float64: squared discrete transport distance, i.e. the SOCP objective value.\nρ::Matrix{Float64}: the n × (N+1) density path, ρ[:, i+1] at time i/N.\nm::Matrix{Float64}: the |E| × N momentum path.\nm0::Vector{Float64}: m[:, 1], the initial momentum (convenience).\nφ0::Vector{Float64}, φ1::Vector{Float64}: the discrete potentials at the two endpoints, read off the duals of the endpoint constraints ρ[:,1] == ρA and ρ[:,N+1] == ρB and divided by π, so that they are exactly the gradient of W2 with respect to each endpoint density under the π-weighted pairing: δW2 = ⟨φ0, δρA⟩_π + ⟨φ1, δρB⟩_π. This is the discrete Hamilton–Jacobi potential (m ≈ -½ θ ∇φ in the continuum limit; see _geodesic_block!), and is the quantity the potential-based analysis Gram matrix (analyze_socp) is built from. Each is defined only up to an additive constant; JuMP/Clarabel return the representative with ⟨φ, 1⟩ fixed by the solver, which is harmless since only ∇φ is ever used.\nstatus: JuMP termination status of the solve.\nsolvetime::Float64: wall-clock solve time in seconds.\n\n\n\n\n\n","title":"GraphTransportation.GeodesicSolution"},{"category":"type","location":"api/#GraphTransportation.MarkovGraph","page":"API","text":"MarkovGraph\n\nGraph primitive for the SOCP formulation.\n\nStores an undirected graph together with a reversible Markov chain on it, using a per-edge (rather than per-node-pair-matrix) representation: momenta, potential gradients, and other edge quantities are each a single Vector{Float64} of length |E|, with an arbitrary but fixed orientation per edge. Antisymmetry of edge fields (Lemma 2.4 of Erbar et al.) is therefore hard-coded into the representation.\n\nFields:\n\nn::Int: number of vertices.\nE::Vector{Tuple{Int,Int}}: oriented edge list, E[e] = (x, y) meaning the edge is oriented from x to y.\nπ::Vector{Float64}: stationary distribution.\nQ::SparseMatrixCSC{Float64,Int}: transition rate matrix.\nκ::Vector{Float64}: edge weights κ[e] = Q[x,y] * π[x] for e = (x,y), which by reversibility equals Q[y,x] * π[y].\nD::SparseMatrixCSC{Float64,Int}: n × |E| incidence matrix with D*m == graph_divergence(G, m) for any edge field m, cached at construction so repeated divergences (e.g. one per SOCP time step) don't rebuild it. D[x,e] = -Q[x,y], D[y,e] = Q[y,x] for edge e = (x,y).\nmean::AdmissibleMean: the mobility θ(s, t) of the transport metric on this graph (GeometricMean() by default; see AdmissibleMean). The mean is part of the geometry, like the graph itself, so it lives here rather than as a per-call option: every geodesic, barycenter and analysis computed from the same MarkovGraph uses the same metric, and a target synthesized on one graph is analysed with the mean it was made with. Chambolle-Pock supports only the geometric mean; the SOCP needs QuadLogMean rather than LogarithmicMean; the shooting maps accept all of them.\n\n\n\n\n\n","title":"GraphTransportation.MarkovGraph"},{"category":"function","location":"api/#GraphTransportation.geodesic_socp","page":"API","text":"geodesic_socp(G::MarkovGraph, ρA, ρB; N=10, optimizer=Clarabel.Optimizer, silent=true, check=true) -> GeodesicSolution\n\nCompute the discrete transport geodesic between densities ρA and ρB on G as a single second-order-cone program, rather than via the Chambolle-Pock primal-dual iteration (discrete_transport). Returns the squared distance W2 = ‖ρA - ρB‖_𝒲²; the metric distance is sqrt(W2).\n\nThe mobility θ is the graph's G.mean: GeometricMean() (default), ArithmeticMean(), HarmonicMean() or QuadLogMean(K) (the logarithmic mean by Gauss–Legendre quadrature, K power cones per edge and time step; a graph built with LogarithmicMean() has no conic form and errors here, use MarkovGraph(G; mean=QuadLogMean(8))). See _mean_cone! for the conic representations and AdmissibleMean for the theory.\n\nN is the number of time-discretization intervals (h = 1/N); the returned ρ has N+1 columns and m has N columns. optimizer is any solver JuMP can dispatch to that supports rotated second-order cone constraints (Clarabel by default). With check=true (default) a solve that does not reach OPTIMAL/ALMOST_OPTIMAL throws rather than returning the solver's last iterate (see _check_solved).\n\n\n\n\n\n","title":"GraphTransportation.geodesic_socp"},{"category":"function","location":"api/#GraphTransportation.barycenter_socp","page":"API","text":"barycenter_socp(G, refs, λ; N=10, optimizer=Clarabel.Optimizer, silent=true, check=true)\n    -> (ν::Vector{Float64}, J::Float64, geodesics::Vector{GeodesicSolution})\n\nCompute the discrete transport barycenter of reference measures refs (a vector of probability densities on G, one per reference) with weights λ as a single joint second-order-cone program, rather than by intrinsic gradient descent (barycenter). Replicates the geodesic SOCP block (_geodesic_block!) once per reference with λ[i] > 0, sharing a single free right endpoint ν across all of them.\n\nReturns:\n\nν: the barycenter, i.e. the shared optimal right endpoint.\nJ: the optimal objective value, Σᵢ λᵢ 𝒲_h²(refs[i], ν).\ngeodesics: one GeodesicSolution per reference with λ[i] > 0 (in the same relative order as refs), giving the geodesic from that reference to ν. Each carries its endpoint potentials (φ0 at the reference, φ1 at ν, both for the unweighted geodesic — the λ[i] factor is divided out of the duals). KKT stationarity of the joint program with respect to ν is exactly Σᵢ λᵢ φ1ᵢ = const on the support of ν, which is the stationarity condition the potential-based analysis (analyze_socp) checks.\n\nReferences with λ[i] == 0 are dropped entirely rather than solved with a zero weight. The mobility is G.mean, and check behaves, as in geodesic_socp. λ must be a probability vector (λ .>= 0, sum(λ) ≈ 1).\n\n\n\n\n\n","title":"GraphTransportation.barycenter_socp"},{"category":"function","location":"api/#GraphTransportation.analyze_socp","page":"API","text":"analyze_socp(G, target, refs; N=10, optimizer=Clarabel.Optimizer,\n             convention=:potential, compute_condition=false, return_system=false)\n    -> λ̂ (or (λ̂, A))\n\nThe :socp analysis backend: recover the barycentric coordinates of target with respect to refs by solving, for each reference, the geodesic SOCP from target to it (geodesic_socp), building a p × p Gram matrix of the resulting tangent vectors at target, and solving the simplex QP min_{λ∈Δ} λᵀAλ shared with the existing analysis (solve_barycentric_coordinates_qp).\n\nconvention selects which tangent-vector representation the Gram matrix is built from:\n\n:potential (default): the Riemannian Gram matrix at target, A_ij = Σ_e κ_e θ(target)_e (∇φ_i)_e (∇φ_j)_e, with φ_i the endpoint potential GeodesicSolution.φ0 — the exact gradient of 𝒲_h²(target, ref_i) with respect to target. Since a barycenter_socp solution is, by its KKT conditions, exactly a point where Σᵢ λᵢ φ_i = const on its support, this recovers the synthesis weights of an SOCP barycenter to solver tolerance at any N, including N=2.\n:momentum: the earlier implementation, which used the initial momentum m0 as the tangent vector with the dense metric_tensor(target) weighting, exactly as the Chambolle-Pock-based analysis does. m0 lives on the first time interval (m0 = -(1/2h) θ(ρ̄_{1/2}) ∇ψ_{1/2}, with the midpoint density and the half-step potential), so it is only an O(h) proxy for the endpoint potential, and Σᵢ λᵢ m0ᵢ ≈ 0 is not the stationarity condition the SOCP barycenter satisfies. At coarse N this mismatch can be large (tens of percent recovery error at N=2 on irregular graphs). Kept for comparison experiments only.\n\nThe mobility for both the geodesic solves and the Gram-matrix weighting is G.mean; recovery is exact for a target synthesized on the same graph (same mean).\n\nrefs is a vector of reference probability densities on G. Returns the recovered weight vector λ̂ (from Convex.jl/SCS on the small p × p Gram matrix), or (λ̂, A) if return_system=true.\n\n\n\n\n\n","title":"GraphTransportation.analyze_socp"},{"category":"function","location":"api/#GraphTransportation.hamiltonian","page":"API","text":"hamiltonian(G::MarkovGraph, ρ, φ) -> Float64\n\nH(ρ,φ) = ½ Σ_e θ(ρ_x,ρ_y) (∇φ)_e² κ_e. Requires ρ strictly positive (see ρ_floor).\n\n\n\n\n\n","title":"GraphTransportation.hamiltonian"},{"category":"function","location":"api/#GraphTransportation.hamiltonian_flow","page":"API","text":"hamiltonian_flow(G::MarkovGraph, ρ, φ) -> (ρ̇, φ̇)\n\nEquations of motion:\n\nρ̇(x) = Σ_y θ(ρ(x),ρ(y)) (φ(x)-φ(y)) Q(x,y)  =  -div(θ(ρ)∘∇φ)(x)\nφ̇(x) = -½ Σ_y ∂₁θ(ρ(x),ρ(y)) (φ(x)-φ(y))² Q(x,y)\n\nwith θ = G.mean and ∂₁θ = partial_s(G.mean, ·, ·) (for the geometric mean ½√(t/s), which divides by ρ(x); hence the positivity floor). Requires ρ strictly positive.\n\n\n\n\n\n","title":"GraphTransportation.hamiltonian_flow"},{"category":"function","location":"api/#GraphTransportation.integrate_hamiltonian","page":"API","text":"integrate_hamiltonian(G::MarkovGraph, ρ0, φ0; nsteps=150, T=1.0, floor_rtol=1e-6)\n    -> (ρ_path, φ_path)\n\nIntegrate the Hamiltonian flow forward from (ρ0, φ0) over [0, T] using fixed-step classical RK4 rather than a symplectic integrator: over a unit time interval at 100-200 steps, symplecticity is a nicety, not a requirement. Returns the full paths as n × (nsteps+1) matrices.\n\nPositivity guard: if any density in the proposed next step would fall below ρ_floor(G; rtol=floor_rtol), the step is halved (up to 4 times) before giving up and throwing PositivityFloorError, which callers should catch and act on (fall back to geodesic_socp or mollify).\n\n\n\n\n\n","title":"GraphTransportation.integrate_hamiltonian"},{"category":"function","location":"api/#GraphTransportation.ρ_floor","page":"API","text":"ρ_floor(G::MarkovGraph; rtol=1e-6) -> Float64\n\nMinimum density the shooting ODE machinery will tolerate: rtol relative to min(π) (every entry point asserts minimum(ρ) > ρ_floor). Below this, θ_geo and its partial derivative ∂₁θ_geo(s,t) = ½√(t/s) are numerically unsafe (division by a near-zero s), and the caller should fall back to geodesic_socp or a mollified approximation (log_map_mollified) instead.\n\n\n\n\n\n","title":"GraphTransportation.ρ_floor"},{"category":"type","location":"api/#GraphTransportation.PositivityFloorError","page":"API","text":"PositivityFloorError <: Exception\n\nThrown by integrate_hamiltonian when a trajectory would drive some density below ρ_floor even after repeated step bisection. log_map catches exactly this type (and nothing broader) to damp its initial guess and shorten line-search steps; any other exception from the flow propagates.\n\n\n\n\n\n","title":"GraphTransportation.PositivityFloorError"},{"category":"function","location":"api/#GraphTransportation.weighted_laplacian","page":"API","text":"weighted_laplacian(G::MarkovGraph, ν) -> SparseMatrixCSC\n\nThe n × n weighted graph Laplacian L_θ(ν) = ∇ᵀ Diag(κ ∘ θ(ν)) ∇, i.e. (L φ)(x) = Σ_y κ_{xy} θ(ν_x, ν_y) (φ(x) − φ(y)). Symmetric positive semidefinite with kernel spanned by the constants, and related to the Hamiltonian flow by π ∘ ρ̇ = L_θ(ν) φ (equivalently ρ̇ = −div(θ(ν)∘∇φ), see hamiltonian_flow).\n\n\n\n\n\n","title":"GraphTransportation.weighted_laplacian"},{"category":"function","location":"api/#GraphTransportation.solve_weighted_laplacian","page":"API","text":"solve_weighted_laplacian(G::MarkovGraph, ν, b) -> φ\n\nSolve L_θ(ν) φ = b for the unique solution in the gauge ⟨φ, 1⟩_π = 0. b must be orthogonal to the constants (sum(b) ≈ 0), which every right-hand side arising here is (π ∘ (target − ν) for two probability densities, or ∇ᵀ(κ ∘ m) for any momentum). Implemented via the rank-one regularization (L + π πᵀ) φ = b: for such b this has the same solution as L φ = b and enforces the gauge automatically.\n\n\n\n\n\n","title":"GraphTransportation.solve_weighted_laplacian"},{"category":"function","location":"api/#GraphTransportation.momentum_to_potential","page":"API","text":"momentum_to_potential(G::MarkovGraph, ν, m) -> φ\n\nRecover the potential φ with m = θ(ν) ∘ ∇φ (gauge ⟨φ, 1⟩_π = 0) from a momentum edge vector m, by solving L_θ(ν) φ = ∇ᵀ(κ ∘ m). If m is not exactly a gradient field this returns the θ(ν)-weighted least-squares projection, i.e. the potential of the gradient part of m in the Hodge sense.\n\n\n\n\n\n","title":"GraphTransportation.momentum_to_potential"},{"category":"function","location":"api/#GraphTransportation.exp_map","page":"API","text":"exp_map(G::MarkovGraph, ν, tangent; t=1.0, nsteps=150, kind=:auto, floor_rtol=1e-6) -> ρ_end\n\nThe Riemannian exponential map at ν. Integrates the Hamiltonian flow (integrate_hamiltonian) from (ν, φ0) for time t and returns the endpoint density.\n\ntangent is either a potential φ0 ∈ Rⁿ (kind=:potential) or a momentum m0 ∈ R^{|E|} (kind=:momentum), in which case φ0 is recovered first with momentum_to_potential. With kind=:auto (default) the kind is inferred from the length, which is ambiguous when n == |E| (e.g. a cycle); then kind must be given. The potential is gauge-fixed to ⟨φ0, 1⟩_π = 0 before integrating (this does not change the flow's ρ).\n\nRequires ν strictly positive (see ρ_floor); errors, rather than returning garbage, if the flow hits the positivity floor before time t.\n\n\n\n\n\n","title":"GraphTransportation.exp_map"},{"category":"function","location":"api/#GraphTransportation.log_map","page":"API","text":"log_map(G::MarkovGraph, ν, target; φ0_init=nothing, tol=1e-9, maxiters=50, nsteps=150,\n        floor_rtol=1e-6, verbose=false) -> (; φ0, m0, W2, iters, residual)\n\nThe Riemannian logarithm at ν, by single shooting. Solves F(φ0) := ρ(1; ν, φ0) − target = 0 with a damped Newton iteration over the mean-zero potentials (n − 1 unknowns, the gauge ⟨φ0, 1⟩_π = 0 and the mass constraint each removing one dimension), with the Jacobian computed by ForwardDiff through integrate_hamiltonian and a backtracking line search on ‖F‖_π.\n\nInitialization is the linearized geodesic L_θ(ν) φ0 = π ∘ (target − ν), exact to first order in target − ν, unless φ0_init (a potential) is given, e.g. from a previous solve at a nearby base point. This is the warm-start mechanism; no (base, target)-keyed cache is kept, callers hold their own φ0. Cold starts on interior data take 2–4 Newton iterations; the initial guess is damped by halving if it overshoots the positivity floor (far-apart concentrated endpoints, ~9 iterations).\n\nNear the positivity floor the integrator's step bisection makes the residual piecewise-smooth in φ0 (ForwardDiff differentiates whichever branch the trajectory took), so Newton can stall at the scale of those jumps; this is the failure mode for strongly mollified boundary data (ε ≲ 1e-4) and is reported as a line-search failure. Multiple shooting is not implemented.\n\nReturns the potential φ0, the momentum m0 = θ(ν) ∘ ∇φ0, the squared distance W2 = 2H(ν, φ0), the Newton iteration count, and the final residual ‖F‖_π. Errors if the iteration has not reached tol after maxiters steps, or if the shooting trajectory persistently hits the positivity floor (fall back to geodesic_socp, or mollify with log_map_mollified). Requires ν and target strictly positive (see ρ_floor).\n\n\n\n\n\n","title":"GraphTransportation.log_map"},{"category":"function","location":"api/#GraphTransportation.log_map_mollified","page":"API","text":"log_map_mollified(G::MarkovGraph, ν, target; εs=(1e-2, 1e-3, 1e-4), tol=1e-7, kwargs...)\n    -> (; W2, W, φ0, m0, fit, εs, Ws, approximate=true)\n\nlog_map for endpoints with zero or near-zero entries, where shooting cannot be run directly. Both endpoints are mollified toward the uniform density, ρ_ε = (1−ε)ρ + ε·𝟙 (still a probability density), log_map is run for each ε in εs (warm-starting each from the previous, coarser ε), and the distance is extrapolated to ε → 0 by a least-squares fit W(ε) ≈ W₀ + a·√ε (the mollification perturbs the endpoints by O(√ε) in 𝒲). Returns the extrapolated W and W2 = W², plus the potential/momentum from the smallest solved ε (at the mollified base point ν_ε, so only approximately a tangent at ν), the fit coefficients (W₀, a), and the raw per-ε distances. Levels whose shooting fails (very small ε makes the near-boundary geodesic stiff) are skipped with a warning as long as two remain. Results are flagged approximate=true; for boundary-supported data geodesic_socp needs no such fallback and is the reference. Empirically the raw W at the smallest ε is often already as accurate as the extrapolation: on 5x5-grid probes the mollification error decayed faster than √ε (successive differences shrank by ~7x per decade of ε, vs. ~3.2x predicted), and the fit over-corrected by 0.3–0.5% while the raw smallest-ε value was within 0.05–0.15% of the SOCP reference. The √ε model is kept as specified; both values are returned so the numerics can be compared with the theory. The shooting tolerance tol defaults to a looser 1e-7 here (the result is approximate anyway, and the near-boundary trajectories make the last digits of the residual hard to reach). Remaining kwargs go to log_map.\n\n\n\n\n\n","title":"GraphTransportation.log_map_mollified"},{"category":"function","location":"api/#GraphTransportation.analyze_shooting","page":"API","text":"analyze_shooting(G::MarkovGraph, target, refs; nsteps=150, tol=1e-9, φ0_inits=nothing,\n                 compute_condition=false, return_system=false) -> λ̂ (or (λ̂, A))\n\nThe :shooting analysis backend: like analyze_socp, but each reference's potential comes from log_map(G, target, ref) (the Hamiltonian velocity potential φ0 at target) instead of the geodesic SOCP's endpoint dual. The Gram matrix and simplex QP are shared with analyze_socp (internal helper potential_gram_qp). Requires strictly positive target and refs; a failure on any one reference propagates (there is no per-reference fallback to the SOCP), and the same weighted Laplacian is refactored once per reference, which is negligible next to the Newton solves.\n\nφ0_inits, if given, is a vector of warm-start potentials, one per reference.\n\nNote that this checks stationarity in the Hamiltonian flow's (RK4, nsteps) discretization, which differs from both barycenter_socp's and the Chambolle-Pock descent's. A barycenter synthesized by either of those is therefore recovered only to O(h) in that method's time step, not to solver tolerance — see the analyze_socp docstring for why synthesis and analysis conventions must match.\n\n\n\n\n\n","title":"GraphTransportation.analyze_shooting"},{"category":"function","location":"api/#GraphTransportation.discrete_transport","page":"API","text":"discrete_transport(Q, μ, ν; N=64, σ=0.5, τ=0.5, maxiters=2^16, tol=1e-10,\n                   progress=false, initialization=nothing) -> ErbarBundle\n\nCompute the discrete transport geodesic from μ to ν on the graph defined by the Markov transition matrix Q, using the Chambolle-Pock primal-dual algorithm of Erbar et al. 2020.\n\nReturns an ErbarBundle encoding the full geodesic; its vector.m[1,:,:] field is the initial tangent vector (logarithmic map at μ), and action(result) gives the squared discrete transport distance.\n\nArguments\n\nQ: row-stochastic Markov transition matrix defining the graph\nμ, ν: probability densities w.r.t. the stationary distribution of Q\nN: number of time steps for the geodesic discretisation (default 64)\nσ, τ: Chambolle-Pock step sizes; must satisfy σ·τ < 1 (default 0.5)\nmaxiters: maximum Chambolle-Pock iterations (default 65536)\ntol: convergence tolerance on the density change between iterates\nprogress: show a progress spinner if true\ninitialization: an ErbarBundle from a prior solve to warm-start from\n\n\n\n\n\n","title":"GraphTransportation.discrete_transport"},{"category":"function","location":"api/#GraphTransportation.action","page":"API","text":"action(u::ErbarBundle) -> Float64\n\nCompute the discrete action functional of the curve encoded in u, as defined in equation (18) of Erbar et al. 2020:\n\nA(ρ, m) = (h/2) · Σ_t Σ_{x,y} (m[t,x,y]² / θ[t,x,y]) · Q[x,y] · π[x]\n\nwhere h = 1/N is the time step.  The integrand uses the convention m²/θ = 0 when m = θ = 0, and +Inf when m ≠ 0 and θ = 0.\n\nThe square root of the action is the discrete Wasserstein distance between the boundary measures u.cache.μ and u.cache.ν.\n\n\n\n\n\n","title":"GraphTransportation.action"},{"category":"function","location":"api/#GraphTransportation.sinkhorn_barycenter","page":"API","text":"sinkhorn_barycenter(coords, measures, target, cost, epsilon; iters=256)\n\nCompute the Wasserstein barycenter of measures with weights coords using the Sinkhorn algorithm with regularization epsilon.\n\n\n\n\n\n","title":"GraphTransportation.sinkhorn_barycenter"},{"category":"function","location":"api/#GraphTransportation.simplex_regression","page":"API","text":"simplex_regression(measures, target, cost, epsilon; iters=256, α0=zeros(S), optim_options=Optim.Options())\n\nWasserstein barycentric coordinates of target with respect to the columns of measures (Bonneel, Peyré & Cuturi 2016, §4.3): minimize E_L(λ) = ½‖P^(L)(λ) − target‖² over the simplex by L-BFGS on α with λ = softmax(α), using the gradient from sinkhorn_differentiate with the softmax Jacobian applied. α0 = 0 is the paper's λ0 = 1/S. iters is the Sinkhorn iteration budget used for both the objective and the gradient. Returns λ̂ ∈ Δ.\n\n\n\n\n\n","title":"GraphTransportation.simplex_regression"},{"category":"function","location":"api/#GraphTransportation.ground_cost","page":"API","text":"ground_cost(G::MarkovGraph, rule::Symbol; t=graph_diameter(G), normalize=true) -> Matrix{Float64}\n\nA ground cost matrix on the nodes of G for the entropic (Sinkhorn) barycenter, barycenter(...; method=:sinkhorn):\n\n:shortest_path: squared BFS hop count (edges of G unweighted).\n:diffusion: squared diffusion distance at time t under the chain G.Q, D_t(x,y)² = Σ_z (Qᵗ[x,z] − Qᵗ[y,z])² / π(z). t defaults to the graph diameter so no pair has zero distance. This uses the chain G.Q itself, which coincides with the random walk on the adjacency only for unweighted chains.\n\nWith normalize=true (default) the matrix is divided by its maximum so entries lie in [0, 1], which keeps the kernel exp(-cost/epsilon) from underflowing at the usual epsilon. Throws if the graph is disconnected.\n\n\n\n\n\n","title":"GraphTransportation.ground_cost"},{"category":"function","location":"api/#GraphTransportation.graph_diameter","page":"API","text":"graph_diameter(G::MarkovGraph) -> Int\n\nLargest BFS hop count between any two nodes, treating the edges of G as unweighted.\n\n\n\n\n\n","title":"GraphTransportation.graph_diameter"},{"category":"function","location":"api/#GraphTransportation.markov_chain_from_edge_list","page":"API","text":"markov_chain_from_edge_list(E) -> (Q, π)\n\nConstruct the uniform random walk Markov chain on the graph defined by the edge list E.  Each element of E is a tuple (i, j) representing an undirected edge.  All edges are given equal weight (unweighted graph), so the transition probability from node x to each neighbor is 1 / degree(x). The stationary distribution satisfies π[x] ∝ degree(x).\n\nReturns (Q, π) where Q is the row-stochastic transition matrix and π is the stationary distribution vector.\n\n\n\n\n\n","title":"GraphTransportation.markov_chain_from_edge_list"},{"category":"function","location":"api/#GraphTransportation.markov_chain_from_adjacency_matrix","page":"API","text":"markov_chain_from_adjacency_matrix(A) -> (Q, π)\n\nConstruct the uniform random walk Markov chain from a binary adjacency matrix A.  Equivalent to markov_chain_from_weight_matrix(A).  The transition matrix is Q[x,y] = A[x,y] / degree(x) and the stationary distribution is π[x] ∝ degree(x).\n\nReturns (Q, π).\n\n\n\n\n\n","title":"GraphTransportation.markov_chain_from_adjacency_matrix"},{"category":"function","location":"api/#GraphTransportation.markov_chain_from_weight_matrix","page":"API","text":"markov_chain_from_weight_matrix(W) -> (Q, π)\n\nConstruct a weighted random walk Markov chain from a non-negative symmetric weight matrix W.  The transition probability from node x to node y is Q[x,y] = W[x,y] / sum(W[x,:]), and the stationary distribution is π[x] = sum(W[x,:]) / sum(W).\n\nReturns (Q, π).\n\n\n\n\n\n","title":"GraphTransportation.markov_chain_from_weight_matrix"},{"category":"function","location":"api/#GraphTransportation.stationary_from_transition","page":"API","text":"stationary_from_transition(Q) -> π\n\nCompute the stationary distribution of a row-stochastic transition matrix Q by solving the linear system (Q' - I)π = 0 with the normalisation constraint sum(π) = 1.  The overdetermined system is assembled and solved via least squares (\\).\n\n\n\n\n\n","title":"GraphTransportation.stationary_from_transition"},{"category":"function","location":"api/#GraphTransportation.triangle_markov_chain","page":"API","text":"triangle_markov_chain() -> (Q, π)\n\nConstruct the uniform random walk Markov chain on the 3-cycle (triangle).\n\n\n\n\n\n","title":"GraphTransportation.triangle_markov_chain"},{"category":"function","location":"api/#GraphTransportation.square_markov_chain","page":"API","text":"square_markov_chain() -> (Q, π)\n\nConstruct the uniform random walk Markov chain on the 4-cycle (square).\n\n\n\n\n\n","title":"GraphTransportation.square_markov_chain"},{"category":"function","location":"api/#GraphTransportation.cube_markov_chain","page":"API","text":"cube_markov_chain() -> (Q, π)\n\nConstruct the uniform random walk Markov chain on the 3-cube (8 nodes, 12 edges). Returns (Q, π) where Q is the transition matrix and π the stationary distribution.\n\n\n\n\n\n","title":"GraphTransportation.cube_markov_chain"},{"category":"function","location":"api/#GraphTransportation.hypercube_markov_chain","page":"API","text":"hypercube_markov_chain() -> (Q, π)\n\nConstruct the uniform random walk Markov chain on the 4-cube (16 nodes, 32 edges).  All edges have equal weight, so the stationary distribution is uniform.\n\n\n\n\n\n","title":"GraphTransportation.hypercube_markov_chain"},{"category":"function","location":"api/#GraphTransportation.weighted_hypercube_markov_chain","page":"API","text":"weighted_hypercube_markov_chain() -> (Q, π)\n\nConstruct a randomly weighted random walk Markov chain on the 4-cube (16 nodes, 32 edges).  Edge weights are drawn uniformly from {1, …, 10} and symmetrised, so the stationary distribution is non-uniform.  Results vary across calls.\n\n\n\n\n\n","title":"GraphTransportation.weighted_hypercube_markov_chain"},{"category":"function","location":"api/#GraphTransportation.grid_markov_chain","page":"API","text":"grid_markov_chain() -> (Q, π)\n\nConstruct the uniform random walk Markov chain on a 9-node wheel-like graph: an 8-cycle (nodes 1–8) with a central hub (node 9) connected to the even nodes.\n\n\n\n\n\ngrid_markov_chain(n) -> (Q, π)\n\nConstruct the uniform random walk Markov chain on the n × n grid graph (n² nodes with nearest-neighbour edges).\n\n\n\n\n\n","title":"GraphTransportation.grid_markov_chain"},{"category":"function","location":"api/#GraphTransportation.triangular_prism_markov_chain","page":"API","text":"triangular_prism_markov_chain() -> (Q, π)\n\nConstruct the uniform random walk Markov chain on the triangular prism graph (6 nodes: two triangular faces connected by three edges).\n\n\n\n\n\n","title":"GraphTransportation.triangular_prism_markov_chain"},{"category":"function","location":"api/#GraphTransportation.triangle_with_tail_markov_chain","page":"API","text":"triangle_with_tail_markov_chain() -> (Q, π)\n\nConstruct the uniform random walk Markov chain on a 3-cycle with one pendant edge (node 4 attached to node 3).\n\n\n\n\n\n","title":"GraphTransportation.triangle_with_tail_markov_chain"},{"category":"function","location":"api/#GraphTransportation.T_markov_chain","page":"API","text":"T_markov_chain() -> (Q, π)\n\nConstruct the uniform random walk Markov chain on the T-shaped graph (path 1–2–3 with a branch 2–4).\n\n\n\n\n\n","title":"GraphTransportation.T_markov_chain"},{"category":"function","location":"api/#GraphTransportation.double_T_markov_chain","page":"API","text":"double_T_markov_chain() -> (Q, π)\n\nConstruct the uniform random walk Markov chain on the double-T graph (8 nodes: two T-shaped graphs — nodes 1–4 and nodes 5–8 — connected at their corresponding vertices: 1↔5, 2↔6, 3↔7, 4↔8).\n\n\n\n\n\n","title":"GraphTransportation.double_T_markov_chain"},{"category":"function","location":"api/#GraphTransportation.ma_house_markov_chain","page":"API","text":"ma_house_markov_chain() -> (Q, π)\n\nConstruct the uniform random walk Markov chain on the Massachusetts state house district adjacency graph.  Nodes are legislative districts; edges connect districts that share a non-trivial boundary (point adjacencies are excluded).\n\nReads district geometries from the bundled shapefile at src/experiments/HOUSE2021/HOUSE2021_POLY.shp.  Requires LibGEOS and Shapefile to be loaded.\n\n\n\n\n\n","title":"GraphTransportation.ma_house_markov_chain"},{"category":"function","location":"api/#GraphTransportation.graph_gradient","page":"API","text":"graph_gradient(Q, f) -> Matrix\n\nCompute the graph gradient ∇_G(f), where G is the graph induced by the transition matrix Q and f is a function defined on the nodes.\n\nEntry (i, j) of the result is f[i] - f[j] when the edge (i,j) exists (i.e. Q[i,j] ≠ 0) and zero otherwise.  The matrix is antisymmetric. Asserts that Q and f have compatible dimensions.\n\n\n\n\n\ngraph_gradient(G::MarkovGraph, φ::AbstractVector) -> Vector{Float64}\n\nCompact-edge-vector counterpart of the dense graph_gradient: (∇φ)[e] = φ[x] - φ[y] for the oriented edge e = (x, y).\n\n\n\n\n\n","title":"GraphTransportation.graph_gradient"},{"category":"function","location":"api/#GraphTransportation.add_graph_gradient!","page":"API","text":"add_graph_gradient!(dest, Q, f)\n\nAdd the graph gradient ∇G(f) directly into dest in-place. Equivalent to `dest .+= graphgradient(Q, f)` but with no intermediate array allocation: the loop body writes only to edge positions and skips non-edges via the sparsity check on Q.\n\n\n\n\n\n","title":"GraphTransportation.add_graph_gradient!"},{"category":"function","location":"api/#GraphTransportation.graph_divergence","page":"API","text":"graph_divergence(Q, m) -> Vector\n\nCompute the graph divergence of the V×V edge field m with respect to the graph induced by Q.  Entry i of the result is\n\n(div m)[i] = (1/2) · Σ_j Q[i,j] · (m[j,i] - m[i,j])\n\n\n\n\n\ngraph_divergence(G::MarkovGraph, m::AbstractVector)\n\nCompact-edge-vector counterpart of the dense graph_divergence, specialized to a single scalar per undirected edge (rather than a full antisymmetric V×V matrix). Agrees with the dense version under m_dense[x,y] = m[e], m_dense[y,x] = -m[e], and therefore satisfies the same adjoint identity ⟨φ, div m⟩_π = -⟨∇φ, m⟩_Q (with ⟨m,w⟩_Q := Σ_e κ[e] m[e] w[e]).\n\nImplemented as G.D * m (see the D field), so m may hold Float64s or, e.g., JuMP variables/affine expressions when building an SOCP model.\n\n\n\n\n\n","title":"GraphTransportation.graph_divergence"},{"category":"function","location":"api/#GraphTransportation.graph_divergence!","page":"API","text":"graph_divergence!(out, Q, m_t)\n\nCompute the graph divergence of the V×V edge field m_t into the pre-allocated length-V vector out, with no allocations.\n\n\n\n\n\n","title":"GraphTransportation.graph_divergence!"},{"category":"function","location":"api/#GraphTransportation.laplacian_from_transition","page":"API","text":"laplacian_from_transition(Q) -> Matrix\n\nReturn the graph Laplacian L = Q - D where D is the diagonal degree matrix D[i,i] = Σ_j Q[i,j].\n\n\n\n\n\n","title":"GraphTransportation.laplacian_from_transition"},{"category":"function","location":"api/#GraphTransportation.metric_tensor","page":"API","text":"metric_tensor(ρ, mean=geomean)\n\nReturn the V × V metric tensor matrix for the probability density ρ.\n\nEntry (i, j) is mean(ρ[i], ρ[j]) for i ≠ j and zero on the diagonal. The default mean is the geometric mean, giving the Riemannian metric associated to the graph Wasserstein geometry of Erbar et al. 2020.  Passing mean=logmean gives the metric for the logarithmic-mean variant.\n\n\n\n\n\nmetric_tensor(G::MarkovGraph, ρ::AbstractVector, mean=G.mean) -> Vector{Float64}\n\nCompact-edge-vector counterpart of the dense metric_tensor: θ[e] = mean(ρ[x], ρ[y]) for the oriented edge e = (x, y), with mean defaulting to the graph's own G.mean (any callable (s, t) -> θ can be passed explicitly). Does not include the edge weight κ; the Riemannian inner product of two potential gradients at ρ is ⟨∇φ, ∇ψ⟩_ρ = Σ_e κ[e] θ[e] (∇φ)[e] (∇ψ)[e].\n\n\n\n\n\n","title":"GraphTransportation.metric_tensor"},{"category":"function","location":"api/#GraphTransportation.avg_operator","page":"API","text":"avg_operator(N)\n\nReturn the sparse time-averaging operator of size (N-1) × N.\n\nRow i contains 0.5 at columns i and i+1, so multiplying by a length-N vector computes the average of each consecutive pair.  In the Erbar et al. 2020 discretisation, this maps the node density curve ρ ∈ V{n,h}^1 (N+1 time points) to its time-averaged version ρavg ∈ V{n,h}^0 (N time points) via `avgoperator(N+1) * ρ`.\n\n\n\n\n\n","title":"GraphTransportation.avg_operator"},{"category":"function","location":"api/#GraphTransportation.finite_difference_operator","page":"API","text":"finite_difference_operator(N)\n\nReturn the sparse forward finite-difference operator of size (N-1) × N.\n\nMultiplying by a length-N vector f returns the vector of forward differences [f[2]-f[1], f[3]-f[2], …, f[N]-f[N-1]].\n\n\n\n\n\n","title":"GraphTransportation.finite_difference_operator"},{"category":"type","location":"api/#GraphTransportation.AdmissibleMean","page":"API","text":"AdmissibleMean\n\nAbstract supertype of the means θ(s, t) (mobilities) that define a discrete transport metric: ‖∇φ‖²_ρ = Σ_e κ_e θ(ρ_x, ρ_y) (∇φ)_e². An admissible mean is continuous, symmetric, positively 1-homogeneous, concave, positive on (0,∞)² and normalised so θ(s, s) = s (Maas 2011). Concavity is what makes the action m²/θ jointly convex, hence the geodesic/barycenter programs convex.\n\nEvery concrete mean θ is callable, θ(s, t), and has partial_s(θ, s, t) (the partial derivative in the first argument; partial_t follows by symmetry). Both are generic in the element type so ForwardDiff can differentiate through them.\n\nFor all s, t > 0: HarmonicMean() ≤ GeometricMean() ≤ LogarithmicMean() ≤ ArithmeticMean(). The arithmetic mean is the only one with θ(0, t) ≠ 0, so it is the only one under which mass can flow out of an empty node; see the package documentation on boundary behaviour.\n\n\n\n\n\n","title":"GraphTransportation.AdmissibleMean"},{"category":"type","location":"api/#GraphTransportation.GeometricMean","page":"API","text":"θ(s,t) = √(st). The package's default, following Erbar et al. (2020).\n\n\n\n\n\n","title":"GraphTransportation.GeometricMean"},{"category":"type","location":"api/#GraphTransportation.ArithmeticMean","page":"API","text":"θ(s,t) = (s+t)/2. Does not vanish at an empty node (θ(0,t) = t/2).\n\n\n\n\n\n","title":"GraphTransportation.ArithmeticMean"},{"category":"type","location":"api/#GraphTransportation.HarmonicMean","page":"API","text":"θ(s,t) = 2st/(s+t). The smallest admissible mean of the four; bounded ∂₁θ near zero.\n\n\n\n\n\n","title":"GraphTransportation.HarmonicMean"},{"category":"type","location":"api/#GraphTransportation.LogarithmicMean","page":"API","text":"θ(s,t) = (s−t)/(ln s − ln t) (Λ). The mean for which the heat flow is the gradient flow of the relative entropy (Maas 2011). Evaluated by a series near s = t so that the value and its derivative are smooth there (needed for ForwardDiff through the shooting maps). Not finitely conic-representable; the SOCP uses QuadLogMean instead.\n\n\n\n\n\n","title":"GraphTransportation.LogarithmicMean"},{"category":"type","location":"api/#GraphTransportation.QuadLogMean","page":"API","text":"QuadLogMean(K::Int=8)\n\nGauss–Legendre approximation of the logarithmic mean, Λ_K(s,t) = Σ_k w_k s^{α_k} t^{1−α_k} ≈ ∫₀¹ s^α t^{1−α} dα = Λ(s,t), with K nodes. Λ_K is itself an admissible mean (symmetric, homogeneous, concave, positive), so a transport metric built on it is exact, not approximate; and each term is a power-cone constraint, which is how the SOCP represents the logarithmic mean. Worst relative error against Λ over density ratios up to 1000: 8.5e-4 (K=4), 6e-7 (K=6), 1.2e-10 (K=8), 2e-15 (K=12).\n\n\n\n\n\n","title":"GraphTransportation.QuadLogMean"},{"category":"function","location":"api/#GraphTransportation.partial_s","page":"API","text":"partial_s(θ::AdmissibleMean, s, t)\n\n∂θ/∂s(s, t). By symmetry partial_t(θ, s, t) = partial_s(θ, t, s).\n\n\n\n\n\n","title":"GraphTransportation.partial_s"},{"category":"function","location":"api/#GraphTransportation.partial_t","page":"API","text":"partial_t(θ::AdmissibleMean, s, t)\n\n∂θ/∂t(s, t), i.e. partial_s(θ, t, s) by the symmetry of admissible means.\n\n\n\n\n\n","title":"GraphTransportation.partial_t"},{"category":"function","location":"api/#GraphTransportation.geomean","page":"API","text":"geomean(x, y) -> Real\n\nGeometric mean of real numbers x and y.  Returns -Inf if either argument is negative (convention used by the graph Wasserstein metric tensor).\n\n\n\n\n\n","title":"GraphTransportation.geomean"},{"category":"function","location":"api/#GraphTransportation.logmean","page":"API","text":"logmean(s, t; tol=1e-5) -> Real\n\nLogarithmic mean of real numbers s and t:\n\nL(s, t) = (s - t) / (log s - log t)\n\nReturns -Inf if either argument is negative, 0 if either is zero and the other non-negative, and (s + t) / 2 (first-order Taylor approximation) when |s - t| ≤ tol.\n\n\n\n\n\n","title":"GraphTransportation.logmean"},{"category":"function","location":"api/#GraphTransportation.logmean_partial_s","page":"API","text":"logmean_partial_s(s, t; tol=1e-5)\n\nPartial derivative of the logarithmic mean with respect to s.\n\nFor |s - t| < tol (near the diagonal), returns the limiting value 0.5. Otherwise returns the exact derivative:\n\n∂/∂s[(s - t)/(log s - log t)] = (-s + t + s·log(s/t)) / (s·(log s - log t)²)\n\n\n\n\n\n","title":"GraphTransportation.logmean_partial_s"},{"category":"function","location":"api/#GraphTransportation.logmean_partial_t","page":"API","text":"logmean_partial_t(s, t; tol=1e-5)\n\nPartial derivative of the logarithmic mean with respect to t.\n\nFor |s - t| < tol (near the diagonal), returns the limiting value 0.5. Otherwise returns the exact derivative:\n\n∂/∂t[(s - t)/(log s - log t)] = (s - t - t·log(s/t)) / (t·(log s - log t)²)\n\n\n\n\n\n","title":"GraphTransportation.logmean_partial_t"},{"category":"type","location":"api/#GraphTransportation.ErbarVector","page":"API","text":"ErbarVector{M, T}\n\nAn element of the Hilbert space H described in equation (27) of Erbar et al. 2020, representing a discretised curve (ρ, m) together with the auxiliary variables (θ, ρ_minus, ρ_plus, ρ_avg, q) used by the Chambolle-Pock solver.\n\nType parameters:\n\nM: concrete 2-D array type for node fields (ρ, ρ_avg, q); typically Matrix{Float64}\nT: concrete 3-D array type for edge fields (m, θ, ρ_minus, ρ_plus); typically Array{Float64,3}\n\nParameterising on the concrete types allows Julia to fully specialise all operations on ErbarVector fields in the Chambolle-Pock inner loop.\n\nFields\n\nρ: density curve, shape (N+1) × V\nm: edge flux, shape N × V × V\nθ: dual edge variable, shape N × V × V\nρ_minus, ρ_plus: edge density copies, shape N × V × V\nρ_avg: time-averaged density, shape N × V\nq: dual node variable, shape N × V\n\n\n\n\n\n","title":"GraphTransportation.ErbarVector"},{"category":"type","location":"api/#GraphTransportation.ErbarCache","page":"API","text":"ErbarCache\n\nImmutable container for the graph OT problem metadata: everything that needs to be computed only once given (Q, μ, ν, N).\n\nConstructors:\n\nErbarCache(Q, π, μ, ν, N) — use a pre-computed stationary distribution π\nErbarCache(Q, μ, ν, N) — computes π internally\nErbarCache(existing, μ_new, ν_new) — cheap warm-start copy reusing the cached linear-system factorisations (ceh_sys, avg_sys depend only on Q and N)\n\nFields\n\nQ: Markov transition matrix\nμ, ν: boundary probability densities\nπ: stationary distribution of Q\nN: number of geodesic time steps\nceh_sys: factorised continuity-equation linear system\navg_sys: factorised time-averaging linear system\n\n\n\n\n\n","title":"GraphTransportation.ErbarCache"},{"category":"type","location":"api/#GraphTransportation.ErbarBundle","page":"API","text":"ErbarBundle{M, T}\n\nA pair (cache::ErbarCache, vector::ErbarVector) representing the complete state of a graph OT problem: the problem metadata and the current iterate.\n\nType parameters {M, T} mirror those of ErbarVector so that accessing bundle.vector in hot-path code returns a concretely-typed value, enabling full specialisation of the Chambolle-Pock inner loop without dynamic dispatch.\n\nConstructors:\n\nErbarBundle(Q, μ, ν, N) — compute π internally\nErbarBundle(Q, π, μ, ν, N) — use pre-computed stationary distribution\nErbarBundle(cache, vector) — assemble from existing components\n\n\n\n\n\n","title":"GraphTransportation.ErbarBundle"},{"category":"function","location":"api/#GraphTransportation.combine!","page":"API","text":"combine!(c::ErbarVector, a::ErbarVector, b::ErbarVector, α::Number, β::Number)\n\nCompute the linear combination c = α·a + β·b in-place across all seven component arrays of the ErbarVector (ρ, m, θ, ρminus, ρplus, ρ_avg, q). No intermediate arrays are allocated; all operations use broadcasting with .=.\n\n\n\n\n\ncombine!(c::ErbarBundle, a::ErbarBundle, b::ErbarBundle, α::Number, β::Number)\n\nBundle-level wrapper for combine! on ErbarVectors.  Computes c.vector = α·a.vector + β·b.vector in-place; the cache fields of the bundles are not modified.\n\n\n\n\n\n","title":"GraphTransportation.combine!"},{"category":"function","location":"api/#GraphTransportation.assign!","page":"API","text":"assign!(c::ErbarVector, a::ErbarVector)\n\nCopy all component arrays of a into c in-place (deep value copy, not pointer aliasing).  Equivalent to c = copy(a) but reuses c's existing allocations.  Covers all seven fields: ρ, m, θ, ρminus, ρplus, ρ_avg, q.\n\n\n\n\n\nassign!(c::ErbarBundle, a::ErbarBundle)\n\nBundle-level wrapper for assign! on ErbarVectors.  Copies a.vector into c.vector in-place; the cache fields are not modified.\n\n\n\n\n\n","title":"GraphTransportation.assign!"},{"category":"section","location":"examples/#Examples","page":"Examples","text":"Numerical experiments from the paper \"Computation of optimal transport on discrete metric measure spaces\" (Erbar, Rumpf, Schmitzer, Simon).\n\nEach script is self-contained and writes its output (.jld2 data file and/or .pdf figures) to the directory it is run from.","title":"Examples"},{"category":"section","location":"examples/#Setup","page":"Examples","text":"The examples use a separate Julia environment that includes plotting and data dependencies not required by the package itself.  From the repo root:\n\njulia --project=examples\n\nThen instantiate once:\n\njulia> import Pkg; Pkg.instantiate()\n\nAfter that, run any script with:\n\njulia --project=examples examples/<ScriptName>.jl\n\nMassachusetts experiments additionally require the MA House Districts shapefile from MassGIS.  See the header of MassachusettsBarycenter.jl for download instructions.\n\n","title":"Setup"},{"category":"section","location":"examples/#Experiment-descriptions","page":"Examples","text":"","title":"Experiment descriptions"},{"category":"section","location":"examples/#Synthetic-graph-experiments","page":"Examples","text":"Script Description\nCubeBarycenter.jl Discrete transport barycenter of four measures on the cube graph (8 nodes). Produces a 2×2 reference panel alongside the barycenter.\nHypercubeBarycenter.jl Barycenter on the unweighted and randomly weighted 4-dimensional hypercube (16 nodes), visualised with a Schlegel diagram.","title":"Synthetic graph experiments"},{"category":"section","location":"examples/#ODE-validation","page":"Examples","text":"Script Description\nErbarODE.jl Validates the WGD barycenter against an explicit ODE solution on the two-point space. Compares discrete transport, WGD barycenter, and Euler ODE across 1000 time steps; produces six diagnostic figures.","title":"ODE validation"},{"category":"section","location":"examples/#Geodesic-and-barycenter-recovery","page":"Examples","text":"Script Description\nGeodesicRecovery.jl Tests coordinate recovery on the triangle graph. Synthesises barycenters along a geodesic, recovers barycentric weights, and measures relative error. Includes a randomised trial producing an error histogram.\nSynthesisAnalysis.jl Evaluates synthesis–analysis round-trip accuracy on grid graphs. Computes random barycenters and measures weight-recovery error as a function of iterations.","title":"Geodesic and barycenter recovery"},{"category":"section","location":"examples/#Grid-graph-experiments","page":"Examples","text":"Script Description\nGridComparison.jl Compares WGD barycenter with Sinkhorn barycenters (shortest-path and diffusion-distance costs) on a 7×7 grid.\nInitializationsGrid.jl Benchmarks sensitivity to warm-start initialisation on a 5×5 grid with five reference measures.\nGridStepSizeVariations.jl Sweeps over geodesic step-count parameters on a 4×4 grid, reporting timing and output distance for cold vs. warm starts.","title":"Grid graph experiments"},{"category":"section","location":"examples/#USA-state-graph-experiments","page":"Examples","text":"These experiments use the 49-node contiguous-USA graph (one node per state plus DC) constructed from adjacency.\n\nScript Description\nStatesComparison.jl Side-by-side comparison of WGD, Sinkhorn/shortest-path, and Sinkhorn/diffusion-distance barycenters on the USA graph.\nEntropicComparison.jl Detailed comparison of Sinkhorn barycenters at diffusion-distance costs with varying time parameter t ∈ {2, 4, 8, 16} against the WGD baseline; California, Maine, and Tennessee as reference measures.\nStateInitializations.jl Benchmarks initialisation sensitivity on the USA graph with five geographically concentrated reference measures.\nParameterVariationsUSA.jl Heatmap of coordinate-recovery error across a grid of (geodesic_steps, geodesic_tol) parameter values on the USA graph.\nWarmStartBenchmark.jl Timing comparison of cold vs. warm-started barycenter computation on the USA graph.\nStateStepVariations.jl Investigates synthesis–analysis step consistency: synthesises at N ∈ {4, 16, 32} geodesic steps and analyses at all values, producing a 3×3 error matrix.\nBCMDemo.jl Visualises the Barycentric Coordinate Manifold on the USA graph: seven barycenters arranged in a triangle according to their recovered weights.","title":"USA state graph experiments"},{"category":"section","location":"examples/#Massachusetts-House-district-graph-experiments","page":"Examples","text":"These experiments use the 160-node Massachusetts House of Representatives district graph derived from the 2021 MassGIS shapefile (rook adjacency). Run MassachusettsBarycenter.jl first to generate the cached Markov chain.\n\nScript Description\nMassachusettsBarycenter.jl Computes the barycenter of four geographically concentrated measures on the MA House graph via the joint SOCP (barycenter) at two time resolutions, with coordinate recovery via analysis.\nMassachusettsGeodesic.jl Visualises the discrete transport geodesic between two geographically distant districts (nodes 5 and 145) in five time frames.\n\nBarycenter of four reference measures on the MA House graph:\n\n(Image: MA House barycenter)","title":"Massachusetts House district graph experiments"},{"category":"section","location":"#GraphTransportation.jl","page":"Home","text":"A Julia package for discrete transport geometry on graphs, implementing the framework of Erbar, Rumpf, Schmitzer, and Simon — Computation of optimal transport on discrete metric measure spaces.","title":"GraphTransportation.jl"},{"category":"section","location":"#Overview","page":"Home","text":"Given a graph encoded as a Markov transition matrix Q with stationary distribution π, this package computes:\n\nGeodesics between probability measures on the graph, as a single second-order-cone program (geodesic), or for strictly positive measures by Hamiltonian shooting (log_map / exp_map), which is faster and exact in time\nDiscrete transport barycenters (Fréchet means) as one joint convex program solved to its global optimum (barycenter)\nBarycentric coordinate recovery by solving a quadratic programme on the Gram matrix of the geodesics' initial potentials (analysis, analysis(...; method=:shooting))\nEntropic optimal transport barycenters via the Sinkhorn algorithm, with simplex-regression-based coordinate recovery\n\nEvery function takes method=:socp (default), :shooting, or :chambolle_pock. The last is the paper's Galerkin-discretised Chambolle-Pock solver with gradient-descent barycenters, kept as the reference implementation; the SOCP and shooting paths are the recommended tools: orders of magnitude faster, with certified optimality for barycenters and coordinate recovery that is exact at the synthesis resolution.","title":"Overview"},{"category":"section","location":"#Quick-start","page":"Home","text":"using GraphTransportation\n\n# Two-point graph with its stationary distribution\nQ = [0.0 1.0; 1.0 0.0]\nπ = [0.5, 0.5]\nG = MarkovGraph(Q, π)\n\n# Two Dirac masses (densities with respect to π)\nμ = [2.0, 0.0]\nν = [0.0, 2.0]\n\n# Discrete transport geodesic; W2 is the squared distance\ngeo = geodesic(G, μ, ν; N=20)            # method=:socp by default\ngeo.W2, geo.ρ\n\n# Barycenter of μ and ν with weights (0.75, 0.25), and its recovered coordinates\nbary, J, _ = barycenter(G, [μ, ν], [0.75, 0.25]; N=20)\ncoords     = analysis(G, bary, [μ, ν]; N=20)\n\n# For strictly positive measures, Hamiltonian shooting is faster and exact in time\nρ0 = [1.2, 0.8]; ρ1 = [0.6, 1.4]\ngeodesic(G, ρ0, ρ1; method=:shooting).W2\ntangent = log_map(G, ρ0, ρ1)      # the underlying exp/log maps are also exported\nexp_map(G, ρ0, tangent.φ0) ≈ ρ1   # round trip","title":"Quick start"},{"category":"section","location":"#Graph-constructors","page":"Home","text":"Several standard graphs are provided in CommonGraphs.jl:\n\nFunction Graph\ntriangle_markov_chain() 3-cycle\nsquare_markov_chain() 4-cycle\ncube_markov_chain() 3-cube (8 nodes)\nhypercube_markov_chain() 4-cube (16 nodes)\ngrid_markov_chain(n) n×n grid\ntriangular_prism_markov_chain() triangular prism (6 nodes)\nma_house_markov_chain() MA state house district adjacency\n\nEach returns (Q, π) where Q is the row-stochastic transition matrix and π is the stationary distribution.","title":"Graph constructors"},{"category":"section","location":"#Barycentric-coding-model","page":"Home","text":"The figure below shows the Barycentric Coding Model (BCM) on the 49-node USA contiguous-states graph. Each sub-graph is a discrete transport barycenter (barycenter) whose position in the triangle reflects its recovered barycentric coordinates (analysis) with respect to three reference measures (corners).\n\n(Image: Barycentric coding model on the USA graph)","title":"Barycentric coding model"},{"category":"section","location":"#API-reference","page":"Home","text":"See the Examples page for runnable experiment scripts, and the API Reference page for full documentation of all exported functions.","title":"API reference"}]
}
